# donnée concervées en memoire pour lecture et ecriture
# spring.datasource.url=jdbc:h2:file:/dbmemory

######### memoire volatile########
spring.datasource.url=jdbc:h2:mem:dbmemory
# mem:dbmemory avec chargement des donnée testdb.sql par defaut
spring.sql.init.data-locations=classpath:testdb.sql
# Autorisation activation automatique des schemas hibernate et via script .sql
spring.jpa.defer-datasource-initialization=true
#spring.jpa.hibernate.ddl-auto=update
#########

spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

spring.h2.console.enabled=true

spring.mvc.pathmatch.matching-strategy=ant_path_matcher